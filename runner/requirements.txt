fastapi==0.138.2
starlette==1.3.1
# app.py imports BaseModel/Field directly, so the version fastapi==0.138.2
# resolves is pinned here rather than ridden in as a transitive dependency.
pydantic==2.12.5
httpx==0.28.1
uvicorn[standard]==0.32.0
pytest==8.4.2
